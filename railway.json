{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install && cd .. && npm install && npm run build:production"
  },
  "deploy": {
    "startCommand": "npm run db:push && echo 'Database migration complete, starting server...' && npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 
